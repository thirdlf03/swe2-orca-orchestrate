# swe2-orca-orchestre

Orca + Devin CLI (SWE-2) 無制限前提のマルチエージェント・オーケストレーション
ハーネス。詳細は [AGENTS.md](AGENTS.md) を参照。

```bash
# PATH に入れる(任意)
export PATH="$PWD/bin:$PATH"
orch init        # 対象repoのルートで実行
```
